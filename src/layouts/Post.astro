---
import BaseHtml from './BaseHtml.astro';
import Navbar from '../components/Navbar.astro';

const { content } = Astro.props;
---

<BaseHtml title={`fiorek blog - ${content.title}`}>
    <Navbar bgColor="bg-cyan-800" />
    <main>
        <slot />
    </main>
</BaseHtml>